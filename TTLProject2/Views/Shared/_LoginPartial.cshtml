@using Microsoft.AspNetCore.Identity
@using TTLProject2.Areas.Identity.Data

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
   @*  <li class="nav-item">
        <a id="manage" class="nav-link text-white fs-6" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Xin Chào @UserManager.GetUserName(User)!</a>
    </li> *@
    <li class="nav-item">
        <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button id="logout" type="submit" class="nav-link btn btn-link text-white fs-6 border-0">Đăng Xuất </button>
        </form>
    </li>
}
else
{
    
    <li class="nav-item">
        <a class="nav-link text-dark fs-6 text-white" id="login" asp-area="Identity" asp-page="/Account/Login">ĐĂNG NHẬP</a>
    </li>
}
</ul>
